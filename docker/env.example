# ============================================================
# AI Blog Bot — Environment Configuration
# ============================================================
# Copy this file to .env in the project root:
#   cp docker/env.example .env
#
# Or use the auto-installer which generates this automatically:
#   ./setup.sh --auto --ollama 192.168.1.100
# ============================================================

# ---- Docker Compose Profile ----
# Choose: light | balanced | full
# light    = App only (bring your own MySQL)
# balanced = App + MySQL (recommended)
# full     = App + MySQL + Nginx reverse proxy
COMPOSE_PROFILES=balanced

# ---- Application ----
APP_PORT=3000
NODE_ENV=production
VITE_APP_TITLE=AI Blog Bot

# ---- Database (MySQL) ----
# IMPORTANT: Change these passwords before first deployment!
# The setup.sh script generates secure passwords automatically.
MYSQL_ROOT_PASSWORD=CHANGE_ME_GENERATE_WITH_openssl_rand_hex_16
MYSQL_DATABASE=ai_blog_bot
MYSQL_USER=ai_blog_bot
MYSQL_PASSWORD=CHANGE_ME_GENERATE_WITH_openssl_rand_hex_16
MYSQL_PORT=3306

# Connection URL (auto-generated by setup.sh for balanced/full profiles)
DATABASE_URL=mysql://ai_blog_bot:CHANGE_ME@mysql:3306/ai_blog_bot

# ---- Authentication (Manus OAuth) ----
# IMPORTANT: Generate a unique JWT secret (min 32 chars)
JWT_SECRET=CHANGE_ME_GENERATE_WITH_openssl_rand_hex_32
VITE_APP_ID=
OAUTH_SERVER_URL=https://api.manus.im
VITE_OAUTH_PORTAL_URL=
OWNER_OPEN_ID=
OWNER_NAME=

# ---- Manus Built-in AI (Forge API) ----
# Used as fallback when local LLM is unavailable
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=
VITE_FRONTEND_FORGE_API_KEY=
VITE_FRONTEND_FORGE_API_URL=

# ---- Telegram Bot ----
# REQUIRED: Get token from @BotFather in Telegram
# 1. Open Telegram, search for @BotFather
# 2. Send /newbot and follow instructions
# 3. Copy the token here
TELEGRAM_BOT_TOKEN=

# Bot username (without @) for the landing page link
VITE_TG_BOT_USERNAME=your_bot

# Optional: Comma-separated Telegram user IDs allowed to use the bot
# Leave empty to allow everyone. Get your ID from @userinfobot
TELEGRAM_ALLOWED_USERS=

# ---- Ollama / Local LLM (External VM) ----
# IP or hostname of the machine running Ollama
# Example: http://192.168.1.100:11434
# Leave empty to use only built-in AI
OLLAMA_HOST=
OLLAMA_MODEL=llama3.2

# ---- Hugo Blog API ----
# Your Hugo admin panel URL and API key
# Can also be configured via chat after deployment
HUGO_API_URL=
HUGO_API_KEY=
